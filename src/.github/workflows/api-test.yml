name: API Tests
run-name: Running API tests 🧪
on:
  push:
    branches: [develop, feat-github-actions-init]
  pull_request:
    branches: [main]
jobs:
  build-and-test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: actions/setup-node@v3
      - run: npm ci
      - run: npm test
